{
    "description": "Config unification gate: only the canonical settings module may use os.getenv/os.environ in runtime code",
    "allowed_paths": [
        "common/config/settings.py"
    ],
    "excluded_dirs": [
        ".git",
        ".venv",
        "__pycache__",
        ".pytest_cache",
        ".ruff_cache",
        "logs",
        "hybro_backend.egg-info",
        "multi_agents_backend.egg-info",
        "scripts",
        "tests",
        "docs"
    ],
    "notes": {
        "scripts": "CLI tools may have their own environment variable needs",
        "tests": "Test code legitimately uses os.environ for monkeypatch/conditional skips"
    }
}
